# Cross-scripting

On the feedback page we have a section where we can post comments.

We tried to check for cross scripting vulnerabilities and we got the flag.

The payload is <script>al in name field and <script> in comments field.